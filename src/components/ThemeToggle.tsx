/**
 * ThemeToggle — desativado na v2 (app é light-only).
 * Mantido como no-op pra preservar imports antigos.
 */
export function ThemeToggle(_: { compact?: boolean; className?: string }) {
  return null;
}
